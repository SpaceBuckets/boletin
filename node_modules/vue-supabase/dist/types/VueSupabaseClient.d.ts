import { Vue2, App } from "vue-demi";
import { SupabaseClient, SupabaseClientOptions } from "@supabase/supabase-js";
export declare class VueSupabaseClient extends SupabaseClient {
    constructor(supabaseUrl: string, supabaseKey: string, supabaseOptions?: SupabaseClientOptions);
    install(app: typeof Vue2 | App): void;
}
export declare function createVueSupabase({ supabaseUrl, supabaseKey, supabaseOptions, }: {
    supabaseUrl: string;
    supabaseKey: string;
    supabaseOptions?: SupabaseClientOptions;
}): VueSupabaseClient;
