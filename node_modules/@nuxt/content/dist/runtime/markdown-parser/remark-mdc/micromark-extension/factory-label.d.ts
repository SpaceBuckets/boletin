import type { Effects, State } from 'micromark-util-types';
export default function createLabel(effects: Effects, ok: State, nok: State, type: string, markerType: string, stringType: string, disallowEol?: boolean): (code: number) => (code: number) => void | ((code: number) => void | any);
