import type { Effects } from 'micromark-factory-space';
export declare function sizeChunks(chunks: any[]): number;
export declare function prefixSize(events: any, type: string): number;
/**
 * Calculate line indention size, line indention could be consists of multiple `linePrefix` events
 * @param events parser tokens
 * @returns line indention size
 */
export declare function linePrefixSize(events: any[]): number;
/**
 * Manage token state
 */
export declare const useTokenState: (tokenName: string) => {
    isOpen: boolean;
    /**
     * Enter into token, close previous open token if any
     */
    enter: (effects: Effects) => () => void;
    /**
     * Enter into token only once, if token is already open, do nothing
     */
    enterOnce: (effects: Effects) => () => void;
    /**
     * Exit from token if it is open
     */
    exit: (effects: Effects) => () => void;
};
