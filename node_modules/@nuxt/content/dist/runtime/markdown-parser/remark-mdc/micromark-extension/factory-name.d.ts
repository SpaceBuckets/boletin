import type { Effects, State } from 'micromark-util-types';
export default function createName(effects: Effects, ok: State, nok: State, nameType: string): (code: number) => void | ((code: number) => void | any);
